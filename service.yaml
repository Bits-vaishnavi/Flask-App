apiVersion: v1
kind: Service
metadata:
  name: fitness-service
  namespace: blue-green-deployment
spec:
  type: NodePort
  selector:
    app: fitness-web
    version: blue
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30001
